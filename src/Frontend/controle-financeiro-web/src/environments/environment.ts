/**
 * Ambiente de desenvolvimento. Em produção este arquivo é substituído por
 * `environment.production.ts` pelo `fileReplacements` do angular.json.
 */
export const ambiente = {
  producao: false,
  urlDaApi: 'https://localhost:5001/api/v1',
};
